# seo-devlog Spec

> 상태: **초안 v0.4**. `❓` 표시는 아직 정해지지 않은 항목이며, 결정되면 이 문서를 수정합니다.

---

## 1. 개요

### 1.1 목적
- AI 엔지니어 포지션 취업용 **포트폴리오와 기술블로그**를 만듭니다.
- GitHub Pages에 정적 사이트로 배포합니다.

### 1.2 컨셉
- **ChatGPT 웹 UI를 차용한 포트폴리오**입니다.
- ChatGPT 사이드바 구조를 그대로 매핑합니다.

  | ChatGPT | seo-devlog |
  |---|---|
  | 프로젝트 (폴더) | 내 프로젝트. 폴더 안에 주제별 대화 |
  | 채팅 (최근 대화) | TIL, 기술 글 |
  | 하단 사용자 프로필 | 나에 대한 정보 (소개, 기술 스택, 이력, 연락처, 이력서) |
  | 새 채팅 화면 | 홈 (히어로 애니메이션 + 입력창) |

- 대화를 열면 **질문과 답변 형식으로 프로젝트를 설명**합니다.
  - 질문 예: "이 프로젝트에서 어떤 문제를 해결했어?"
  - 답변에는 기술 내용, 트러블슈팅, 깨달은 점을 담고 **스트리밍으로 출력**합니다.

### 1.3 브랜딩 목표
- "AI 엔지니어가 만든 AI 서비스 같은 포트폴리오"라는 인상을 줍니다.
- 동시에 **UI 감각이 있는 프로덕트 엔지니어**로 보이는 것이 목표입니다.
  - 실제 서비스 수준의 완성도: 스트리밍, 마이크로 인터랙션, 전환 애니메이션, 반응형
  - 디테일: 로딩/빈 상태, 키보드 조작, 포커스 스타일, `prefers-reduced-motion` 대응
  - 콘텐츠만큼 사용감 자체가 포트폴리오가 되도록 합니다.

### 1.4 제약
- 백엔드와 DB가 없습니다. 모든 데이터는 **레포 안의 파일(Markdown/JSON)** 입니다.
- 빌드 시점에 파일을 읽어 정적 HTML/JS로 생성합니다(SSG).
- 공개 레포이므로 초안, 회사 내부 정보, API 키를 커밋하지 않습니다.
- 콘텐츠는 **한국어만** 작성합니다.

---

## 2. 사용자와 시나리오

| 사용자 | 목적 | 핵심 동선 |
|---|---|---|
| 채용 담당자 / 면접관 | 짧은 시간 안에 역량 파악 | 히어로 → 추천 질문 또는 프로젝트 → 하단 프로필(연락처/이력서) |
| 현업 개발자 | 기술 깊이 확인 | 프로젝트 폴더 → 아키텍처/트러블슈팅 대화 |
| 본인 | 콘텐츠 작성과 관리 | `.md` 파일 작성 → push → 자동 배포 |

---

## 3. 화면 구성

### 3.1 전체 레이아웃 (레퍼런스: ChatGPT 웹)

```
┌───────────────────┬──────────────────────────────────────────┐
│ seo-devlog   🔍 ◫ │                                          │
│ ✎ 새 채팅         │                                          │
│ 소개              │         대화 영역 (메시지 목록)          │
│ 기술 스택         │                                          │
│ 이력서            │                                          │
│                   │                                          │
│ 프로젝트          │   👤 이 프로젝트 어떤 문제를 풀었어?     │
│  lewisai          │                                          │
│  ...              │   🤖 ...답변 스트리밍 출력...▍           │
│  더 보기          │                                          │
│                   │                                          │
│ 채팅              │                                          │
│  RAG 청킹 전략    │                                          │
│  ...              │  ┌────────────────────────────────────┐  │
│ ───────────────── │  │ + 무엇이든 물어보세요           ⬆ │  │
│ (●) 이름          │  └────────────────────────────────────┘  │
│     AI Engineer   │                                          │
└───────────────────┴──────────────────────────────────────────┘
```

- 상단 모델 선택 드롭다운은 **넣지 않습니다.**

### 3.2 페이지 목록

| 경로 | 화면 | 설명 |
|---|---|---|
| `/` | 홈 (새 채팅) | WebGL 인트로 → 클릭 시 채팅 UI로 확산(3.4), 입력창, 추천 질문 |
| `/p/[project]` | 프로젝트 페이지 | 프로젝트 소개 + 대화 목록(3.9) |
| `/p/[project]/[chat]` | 프로젝트 대화 | 프로젝트 폴더 안의 주제별 대화 |
| `/c/[slug]` | 단독 대화 | TIL, 기술 글 |
| `/library` | 라이브러리 | 자격증, 수료증, 상장 이미지 모음(3.10) |
| `/404` | 404 | "대화를 찾을 수 없습니다" 형태 |

- 대화마다 **별도 HTML을 생성**합니다. 새로고침 404를 막고 SEO에 유리하기 때문입니다.

### 3.3 사이드바

| 영역 | 내용 | 정렬 / 동작 |
|---|---|---|
| 상단 | 사이트 이름, 검색(🔍), 사이드바 접기(◫), 새 채팅 | 새 채팅을 누르면 홈으로 이동 |
| 바로가기 메뉴 | 소개, 라이브러리, 이력서 | 소개는 프로필 패널을 열고, 라이브러리는 `/library`로 이동하고(3.10), 이력서는 PDF를 새 탭으로 엽니다. 기술 스택은 프로필 패널 안에만 둡니다 |
| 프로젝트 | 프로젝트 폴더 목록, 기본 5개 노출 후 "더 보기" | `order` 오름차순. 클릭하면 프로젝트 페이지로 이동합니다(3.9) |
| 채팅 | TIL, 기술 글 | `date` 내림차순 |
| 하단 프로필 | 아바타, 이름, 한 줄 직함 | 클릭하면 **프로필 패널**을 엽니다(3.7) |

- 현재 열린 대화와 프로젝트를 하이라이트합니다.
- 모바일에서는 드로어로 여닫습니다.
- 검색(🔍)은 입력창 자동완성과 같은 인덱스로 대화 제목과 질문을 검색하는 모달입니다(ChatGPT 검색 모달 형태).

### 3.4 홈 (새 채팅 화면)
- **첫 진입 시 스크롤 랜딩을 보여준 뒤, 끝까지 내리고 클릭하면 WebGL 확산으로 채팅 UI를 드러냅니다.**
- 컨셉: **"AI 엔지니어가 되려다 AI가 되어버린 나"**. 사용자의 질문이 AI의 답변으로 바뀌는 이야기로, 이 사이트가 대화 형식의 기술 블로그라는 걸 설명 없이 전달합니다.
- 장면 (검은 배경, 흰 글자. 스크롤 비율 0~1 기준)

  | 구간 | 장면 | 인터랙션 |
  |---|---|---|
  | 0 | `Seo Dong Hwi` + 스크롤 안내 | 스크롤하면 위로 흐려지며 빠짐 |
  | 0.06–0.26 | 사용자 말풍선 "AI 엔지니어가 되고 싶었습니다." | 스크롤한 만큼 타이핑, 올리면 지워짐 |
  | 0.25–0.47 | "배운 걸 전부 대화로 남겼습니다." + 키워드 구름 + 프로젝트/대화/질문 수 | 키워드가 하나씩 등장, 포인터를 따라 깊이별 패럴랙스, 숫자 카운트업 |
  | 0.46–0.66 | "배울수록 토큰이 되어 갔습니다." + `AI 엔지니어가 되려던 서동휘` | 글자 조각이 차례로 토큰 칩으로 바뀌고 토큰 ID가 붙음 |
  | 0.65–0.84 | "그리고 AI가 되어버렸습니다." + AI 답변 | 생각 중 점 → 답변이 스크롤로 스트리밍 |
  | 0.83–0.93 | "이 블로그는 저와 나누는 대화입니다." + 프로젝트/채팅/프로필 카드 | 카드가 차례로 올라옴 |
  | 0.92–1 | 캔버스에 `Seo Dong Hwi`가 다시 떠오름 + "아무 곳이나 눌러서 대화 시작하기" | 98.5%에 닿아야 클릭·Enter·Space로 진입 가능 |

- **진입**: 끝까지 내리기 전에는 진입할 수 없고, 건너뛰기(Esc)도 없습니다. 진입하면 화면 중앙에 픽셀화된 노이즈 경계의 구멍이 생겨 바깥으로 퍼지고, 구멍 안으로 채팅 UI가 드러납니다(1.6초, SEED `easing`). 구멍이 화면 전체로 퍼지면 오버레이와 WebGL 컨텍스트를 제거합니다.
- 구현
  - `src/components/HeroIntro.astro`: 오버레이 안의 스크롤 컨테이너(900vh) + sticky 무대. 스크립트는 스크롤 비율과 장면별 값(`--v` 보임, `--y` 방향, `--t` 진행도)만 넣고, 움직임은 CSS `transform`/`opacity`/`filter`가 처리합니다. 휠 스크롤이 튀지 않도록 실제 위치를 보간해 따라갑니다.
  - `src/lib/hero-intro.ts`: fragment shader 하나(fbm 노이즈 경계 + 임계값 두 개 + 좌표 양자화). `uTextAlpha`로 마지막 장면의 이름을 띄웁니다. 외부 라이브러리 없음.
  - 문구는 `site.json`의 `landing`에서 읽습니다. 키워드는 프로필 기술 스택 + 콘텐츠 태그 중 기술명(대문자로 시작) + `landing.keywords`, 숫자는 실제 프로젝트·대화·질문 수입니다.
- 표시 여부는 첫 페인트 전에 head 스크립트가 정해서 채팅 UI가 먼저 보였다가 가려지는 깜빡임이 없습니다.
- WebGL을 쓸 수 없으면 같은 장면 뒤 검은 화면이 페이드아웃됩니다.
- `prefers-reduced-motion`이면 장면을 정적인 글로 차례대로 늘어놓고, 끝까지 내린 뒤 클릭하면 페이드로 진입합니다.
- 사이드바나 URL로 다른 페이지에 직접 진입한 경우에는 랜딩을 보여주지 않습니다.
- 같은 세션에서 다시 방문하면 랜딩을 건너뜁니다(`sessionStorage`).
- 추천 질문(`suggested: true`인 대화)을 클릭하면 해당 대화로 이동합니다.

### 3.5 대화 상세
- 메시지는 `user`(질문)와 `assistant`(답변)가 번갈아 나옵니다.
- 답변은 Markdown으로 렌더링합니다.
  - 코드 하이라이팅, 표, 이미지, 링크를 지원합니다.
  - Mermaid 다이어그램은 ❓
- 코드 블록에는 복사 버튼을 둡니다.
- 프로젝트 대화 하단에는 GitHub 레포, 데모 링크, 같은 프로젝트의 다음 대화 링크를 둡니다.
- 메시지마다 앵커(`#m-3`)를 둬서 특정 질문으로 바로 이동할 수 있게 합니다.
- 대화에 들어오거나 질문을 보내면 화면 가장자리가 iOS Siri처럼 한 번 번쩍이고 사라집니다(`src/components/EdgeFlash.astro`). 얇은 링과 넓은 블룸 두 겹, 1.6초, 모션 감소 설정에서는 재생하지 않습니다.

### 3.6 답변 스트리밍 출력
- **HTML은 빌드 시 전부 생성**하고, 클라이언트에서 순서대로 드러내는 방식으로 스트리밍을 연출합니다. 크롤러와 JS 비활성 환경에서는 전체 내용이 그대로 보이므로 SEO를 해치지 않습니다.
- 출력 단위 (제안)
  - 문단과 목록: 글자/토큰 단위로 출력하고 끝에 커서(▍)를 표시합니다.
  - 코드 블록, 표, 이미지: 블록 단위로 페이드인합니다. 깨진 마크업이 보이지 않게 하기 위해서입니다.
- 다음 차례 질문은 하단 입력창에 타이핑되듯 채워집니다. 방문자가 Enter(또는 보내기)를 누르면 질문이 전송되고, 답변이 끝날 때까지 입력창은 잠깁니다. 채우는 도중 키를 누르면 질문이 끝까지 채워집니다.
- 질문 메시지가 먼저 나오고, 짧은 "생각 중" 인디케이터 후에 답변이 출력됩니다.
- 대화에 턴이 여러 개면 턴 순서대로 이어서 출력합니다.
- 화면 하단을 따라 자동 스크롤합니다. 사용자가 위로 스크롤하면 자동 스크롤을 멈춥니다.
- 답변 영역 클릭 또는 "건너뛰기" 버튼으로 즉시 전체를 표시합니다.
- 이미 본 대화는 세션 동안 즉시 표시합니다(`sessionStorage`).
- `prefers-reduced-motion`이면 스트리밍 없이 즉시 표시합니다.

### 3.7 프로필 패널
- 하단 프로필을 클릭하면 모달(모바일은 바텀시트)로 엽니다.
- 내용
  - 사진, 이름, 직함, 한 줄 소개
  - 기술 스택
  - 경력, 학력, 자격, 수상
  - 연락처: 이메일, GitHub, LinkedIn 등
  - 이력서 PDF 보기 버튼
- 데이터는 `src/content/profile` 파일에서 읽습니다(4.1).

### 3.8 입력창: 추천 질문 자동완성
- 빌드 시점에 모든 대화에서 **질문 인덱스**(JSON)를 생성합니다.
  - 인덱스에는 `user` 메시지, 대화 제목, 태그, 대화 경로, 메시지 앵커가 들어갑니다.
- 입력하면 Fuse.js로 퍼지 매칭해서 후보 질문을 드롭다운으로 보여줍니다.
- 후보를 선택하면 해당 대화로 이동하고, 그 질문부터 스트리밍 출력합니다.
- 매칭이 없으면 "아직 준비된 답변이 없어요"와 함께 추천 질문을 보여줍니다.
- 키보드(↑↓, Enter, Esc)로 조작할 수 있어야 합니다.
- 실제 LLM 연동은 이후 확장으로 둡니다(8장).

### 3.9 프로젝트 페이지 (`/p/[project]`)
- ChatGPT의 프로젝트 화면을 레퍼런스로 합니다.
- 구성
  - 상단: 폴더 아이콘과 프로젝트 이름, 한 줄 요약, 기간, 역할, 태그, GitHub/데모 링크
  - (선택) `_project.md` 본문의 소개글
  - 입력창: "○○에 대해 물어보세요". 자동완성 범위를 이 프로젝트의 질문으로 한정합니다.
  - 대화 목록: 대화 제목, 첫 질문 미리보기. `order` 오름차순
- 채용 담당자가 대화를 열지 않고도 프로젝트를 한눈에 파악할 수 있어야 합니다.

### 3.10 라이브러리 (`/library`)
- ChatGPT의 "라이브러리"(이미지 모음)를 레퍼런스로, 자격증·수료증·상장 이미지를 모아 보여줍니다.
- 상단에 `전체 · 자격증 · 수료증 · 상장` 필터 칩을 둡니다. 거르면 격자가 View Transitions로 재배치됩니다.
- 격자: 모바일 2열, 데스크톱 3열. 가로·세로 서류가 섞여도 잘리지 않게 4:3 틀 안에 통째로 넣습니다. 썸네일은 빌드 시 WebP로 만듭니다.
- 이미지를 누르면 라이트박스가 열립니다.
  - 썸네일이 그 자리에서 큰 이미지로 커집니다(View Transitions, 미지원 브라우저는 즉시 전환).
  - 이미지 아래에 종류 · 발급처 · 취득일, 제목, 짧은 설명(마크다운 본문)을 보여줍니다.
  - ← → 키, 좌우 버튼, 모바일 스와이프로 넘기고(필터 결과 안에서만), Esc · 배경 · 닫기 버튼으로 닫습니다.
  - 항목마다 `/library#[slug]` 주소가 생겨 특정 항목을 바로 여는 링크를 보낼 수 있습니다.
- **다운로드는 제공하지 않습니다.** 저장 버튼이 없고, 이미지 우클릭 메뉴와 드래그를 막습니다. 스크린샷까지 막을 수는 없으므로, 개인정보(생년월일, 자격번호, 발급번호 등)는 올리기 전에 이미지에서 가려야 합니다.

---

## 4. 콘텐츠 (데이터) 설계

### 4.1 디렉터리

```
src/content/
├─ projects/                  # 프로젝트 폴더 → /p/[project], /p/[project]/[chat]
│  └─ lewisai/
│     ├─ _project.md          # 프로젝트 메타 (제목, 기간, 역할, 태그, 링크)
│     ├─ 01-overview.md
│     ├─ 02-architecture.md
│     ├─ 03-troubleshooting.md
│     └─ 04-retrospective.md
├─ chats/                     # TIL / 기술 글 → /c/[slug]
│  └─ 2026-09-10-rag-chunking.md
├─ library/                   # 라이브러리 → /library#[slug]
│  ├─ sqld.md                 # 제목, 종류(자격증|수료증|상장), 발급처, 날짜, 이미지 경로 + 본문 설명
│  └─ sqld.png                # 같은 폴더의 이미지 (개인정보는 가린 뒤 저장)
├─ profile.md                 # 프로필 패널 (소개, 기술 스택, 이력, 연락처)
└─ site.json                  # 사이트 이름, 히어로 문구, 추천 질문 등

public/
├─ images/[project]/          # 대화에 쓰는 이미지
├─ avatar.png
└─ resume.pdf
```

- 파일명 앞의 숫자(`01-`)는 정렬용이며 URL slug에서는 뺍니다(`/p/lewisai/overview`).
- 바로가기 메뉴는 별도 콘텐츠 없이 `profile.md`와 `resume.pdf`를 재사용합니다.

### 4.2 파일 형식 (제안)

**프로젝트 메타 (`projects/[project]/_project.md`)**

```md
---
title: LewisAI
summary: RAG 기반 법률 상담 챗봇 (사이드바 툴팁, OG description)
period: 2026-06 ~ 2026-08
role: AI 엔지니어 (백엔드 / RAG 파이프라인)
tags: [RAG, FastAPI, LangChain]
links:
  github: https://github.com/...
  demo: https://...
order: 1
draft: false
---

(선택) 프로젝트 페이지에 보여줄 소개글
```

**대화 파일 (프로젝트 대화, 채팅 공통)**

````md
---
title: 가장 어려웠던 문제
date: 2026-08-01
tags: [retrieval, evaluation]
order: 3                 # 프로젝트 폴더 내 정렬 (채팅은 date 사용)
suggested: true          # 홈 추천 질문에 노출할지
draft: false
---

:::user
이 프로젝트에서 가장 어려웠던 문제는?
:::

:::assistant
법률 문서 검색의 정확도 문제를 해결하려고...

```python
# 코드 예시
```
:::

:::user
그 문제를 어떻게 해결했어?
:::

:::assistant
...
:::
````

- 턴 구분은 `:::user` / `:::assistant` 블록(remark-directive)을 씁니다.
- frontmatter는 스키마(zod)로 검증해 빌드 시점에 오류를 잡습니다.
- `draft: true`인 대화는 프로덕션 빌드에서 제외합니다.
- 이미지는 `public/images/[project]/`에 둡니다.

### 4.3 프로젝트 폴더 권장 대화 구성
프로젝트마다 아래 대화를 기본 틀로 쓰고, 필요하면 더하거나 뺍니다.

| 대화 | 담을 내용 |
|---|---|
| 개요 | 배경, 목표, 기간, 역할, 팀 규모 |
| 아키텍처 | 전체 구조, 기술 스택 선택 이유, 다이어그램 |
| 핵심 구현 | RAG 파이프라인, 프롬프트 설계, 평가 방법 등 |
| 트러블슈팅 | 문제 → 원인 분석 → 해결 → 결과 (주제마다 대화를 따로 만들어도 됨) |
| 성과 | 정확도, 응답 시간, 비용 등 수치 |
| 회고 | 깨달은 점, 다시 한다면 개선할 점 |

---

## 5. 기술 스택

| 항목 | 선택 | 비고 |
|---|---|---|
| 런타임 | **Node 24 LTS** | fnm으로 관리, 프로젝트 루트 `.node-version`. Astro 7은 Node 22.12 이상 필요 |
| 프레임워크 | **Astro 7** | Content Collections로 Markdown 관리, 페이지별 정적 HTML |
| UI 컴포넌트 | **React** (Astro Islands) | 사이드바, 입력창 자동완성, 스트리밍, 프로필 패널, 히어로 |
| 언어 | TypeScript | |
| 스타일 | **Tailwind CSS** | 디자인 토큰(`@theme`)으로 색상, 간격, 폰트 통일 |
| 애니메이션 | Motion (Framer Motion) ❓ | 히어로, 레이아웃 전환, 패널 |
| Markdown | `@astrojs/markdown-remark`(unified) + remark-directive | Astro 7 기본 처리기(Sätteri) 대신 unified를 써서 remark 플러그인을 사용합니다. `:::user` / `:::assistant`는 `src/lib/remark-chat-turns.mjs`가 `<section class="chat-turn" id="m-N">`으로 변환합니다 |
| 코드 하이라이팅 | Shiki `github-light` | 빌드 시 처리. 코드 블록 바탕은 `--color-code` 토큰으로 덮어씁니다 |
| 자동완성 / 검색 | Fuse.js | 빌드 시 생성한 질문 인덱스 사용 |
| 댓글 | 없음 ❓ | 필요하면 giscus |
| 분석 | ❓ | GA / Umami / GoatCounter |

---

## 6. 비기능 요구사항

- **반응형**: 모바일(360px)부터 데스크톱까지 지원합니다. 모바일에서 사이드바는 드로어입니다.
- **색상**: 그레이스케일 단색 팔레트만 씁니다. 파랑 같은 강조 색 없이 명도 차이와 반전(밝은 배경 + 어두운 글자)으로 위계를 표현합니다. **라이트 톤이 기본**입니다(ChatGPT 웹 참고: 흰 바탕 `#FFFFFF`, 사이드바·입력창은 클라우드 그레이 `#EDEDED`). 코드 블록의 문법 강조(`github-light`)만 색을 씁니다. 다크 모드 지원 여부는 ❓
- **아이콘**: UI에 이모지를 쓰지 않습니다. 선형 SVG 아이콘(Lucide 기반, stroke 1.75)만 씁니다(`src/components/Icon.astro`).
- **모션**: SEED Design 모션 파운데이션을 따르고, 토큰은 `src/styles/global.css`에 정의합니다.
  - duration: d1 50ms ~ d6 300ms. 200ms 이하는 마이크로 모션(클릭, 포커스, 색상 변화), 초과는 매크로 모션(드로어, 모달)
  - easing: `easing`(0.35, 0, 0.35, 1)은 이동과 상태 변화, `enter`(0, 0, 0.15, 1)는 등장, `exit`(0.35, 0, 1, 1)은 퇴장
  - 눌림 피드백: 요소 크기와 무관하게 약 2px 축소합니다. `basis = max(높이, 너비 / 4, 24)`, `scale = (basis - 2) / basis`, 150ms
  - 모든 애니메이션은 `prefers-reduced-motion`을 존중합니다. 모션 감소 시 축소 대신 색상 변화로만 피드백하고, 긴 연출은 스킵할 수 있어야 합니다.
- **SEO**: 대화마다 다음을 둡니다.
  - `<title>`, meta description, OG 이미지
  - 사이트 전체에 `sitemap.xml`, `robots.txt`
- **성능**: Lighthouse Performance 90 이상을 목표로 합니다. 히어로 애니메이션이 LCP를 늦추지 않게 합니다.
- **접근성**: 키보드로 사이드바, 입력창, 패널을 조작할 수 있어야 합니다. 스트리밍 영역에는 적절한 `aria-live` 설정을 둡니다.
- **언어**: 한국어만 지원합니다. `<html lang="ko">`

---

## 7. 배포

- GitHub Actions를 씁니다. `main`에 push하면 빌드 후 GitHub Pages에 배포합니다.
- Settings → Pages → Source는 **GitHub Actions**입니다.
- 빌드 결과물 루트에 `.nojekyll`을 둡니다.
- 빌드 결과물(`dist/`)은 커밋하지 않습니다.
- 배포 주소: **`https://seoddhh.github.io`** (base 경로 `/`)
  - GitHub에서 레포 이름을 `seo-devlog` → `seoddhh.github.io`로 변경해야 합니다.
  - 변경 후 로컬 remote도 수정합니다: `git remote set-url origin https://github.com/seoddhh/seoddhh.github.io.git`
  - 로컬 폴더 이름(`seo-devlog`)과 사이트 이름은 그대로 써도 됩니다.
  - `astro.config.mjs`의 `site`는 `https://seoddhh.github.io`로 설정하고 `base`는 설정하지 않습니다.

---

## 8. 범위

### MVP
- [ ] ChatGPT 스타일 레이아웃 (사이드바, 대화 영역, 입력창)
- [ ] Content Collections 스키마 (projects / chats / profile)
- [ ] Markdown 대화 파일 파싱과 렌더링
- [ ] 답변 스트리밍 출력
- [ ] 홈 화면 + 히어로 애니메이션
- [ ] 프로젝트 페이지, 프로젝트 대화, 단독 대화 페이지 정적 생성
- [ ] 프로필 패널
- [ ] 입력창 추천 질문 자동완성, 검색 모달
- [ ] 반응형 + 라이트 테마
- [ ] GitHub Actions 배포
- [ ] 콘텐츠: 프로필 + 프로젝트 1개 이상(폴더 내 대화 3개 이상)

### 이후
- [ ] 실제 LLM 연동 (서버리스 + 내 콘텐츠 기반 RAG)
- [ ] SEO(OG 이미지 자동 생성)
- [ ] 분석 도구
- [ ] 라이트 모드, 영문 버전

---

## 9. 미결정 사항 (Open Questions)

| # | 질문 | 선택지 / 메모 |
|---|---|---|
| Q15 | 프로필에 쓸 이름, 직함, 인사 문구 | `site.json` / `profile.md` 작성 시 결정 |
| - | 라이트 모드 지원, 애니메이션 라이브러리, Mermaid, 댓글, 분석 | 구현 단계에서 결정 |

### 결정 완료

| # | 결정 | 날짜 |
|---|---|---|
| Q1 | 프레임워크: **Astro + React** | 2026-09-15 |
| Q3 | 입력창: **추천 질문 자동완성** (실제 LLM은 이후 확장) | 2026-09-15 |
| Q4 | 콘텐츠 종류: **프로젝트, TIL/기술 글, 소개, 기술 스택/이력, 연락처/이력서** | 2026-09-15 |
| Q5 | 언어: **한국어만** | 2026-09-15 |
| Q6 | 답변 출력: **스트리밍 연출** (3.6) | 2026-09-15 |
| Q7 | 프로젝트 구성: **프로젝트 = 폴더, 주제별 여러 대화** | 2026-09-15 |
| Q8 | 나에 대한 정보: **사이드바 하단 프로필 → 프로필 패널** | 2026-09-15 |
| Q9 | UI 레퍼런스: **ChatGPT 웹, 라이트 기본** (2026-09-18 다크 → 라이트로 변경) | 2026-09-15 |
| Q10 | 모델 선택 드롭다운: **제외** | 2026-09-15 |
| Q12 | 스타일링: **Tailwind CSS** | 2026-09-15 |
| Q2 | 배포 주소: **`https://seoddhh.github.io`** (레포 이름 변경) | 2026-09-15 |
| Q11 | 프로젝트 폴더 클릭: **프로젝트 페이지** (3.9) | 2026-09-15 |
| Q13 | 홈: **WebGL 인트로(Seo Dong Hwi) → 클릭 시 노이즈 원 확산 → 채팅 UI**, 단색 (3.4) | 2026-09-15 |
| Q17 | 사이드바 기술 스택 → **라이브러리**(자격증·수료증·상장 이미지, 라이트박스 설명, 다운로드 없음) (3.10) | 2026-09-19 |
| Q16 | 홈 랜딩: **"AI 엔지니어가 되려다 AI가 되어버린 나" 스크롤 스토리 → 끝까지 내린 뒤 클릭 시 확산 진입**. 세션마다 1회, Esc 건너뛰기 없음, 토큰 칩 연출 (3.4) | 2026-09-19 |
| Q14 | 사이드바 상단: **소개 / 기술 스택 / 이력서 바로가기** | 2026-09-15 |
